# listings/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # Example: add your booking endpoints here
    # path('bookings/', views.BookingViewSet.as_view({'post': 'create'})),
]